[DEPENDENCIES]
# pygbag's filter matches against paths rooted at "/" (see
# pygbag/filtering.py). Entries must begin with "/" to match prefixes.
ignoreDirs = ["/.claude", "/.git", "/.venv", "/build", "/tests", "/scripts", "/pygbag"]
ignoreFiles = ["pygbag.ini", ".gitignore", "requirements.txt"]
